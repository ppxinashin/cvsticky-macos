## 提交规则确认

- [ ] 本次代码开发基于 `dev` 分支或功能分支完成。
- [ ] 本 PR 的目标分支是 `dev`；只有作者本人发布/维护时才允许以 `main` 为目标分支。
- [ ] 已完成必要验证，并在下方填写验证结果。
- [ ] 未直接向 `main` 分支提交代码。

## 变更说明


## 验证结果


## 作者合并确认

`main` 分支只允许仓库作者本人在代码验证通过后手动合并。非作者不得直接提交、推送或合并到 `main`。
